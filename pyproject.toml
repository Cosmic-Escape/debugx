[project]
name = "debugx"
version = "0.1.0"
description = "Smart debugging for Python developers"
authors = [{name="Vivek Verma"}]
dependencies = ["rich"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]